# Currently...

- We have some state called view
- onSelect we call the handleSelect() fn that sets the view to the key
- In our render method we're doing conditional rendering based on this.state.view
